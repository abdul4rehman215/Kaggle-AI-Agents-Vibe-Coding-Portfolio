# Skill Test Artifacts

These files were created while testing the Antigravity Skills codelab.

| File | What it demonstrates |
|---|---|
| `my_script.py` | License header skill applied a Python-compatible header. |
| `product.json` | Input file for the JSON-to-Pydantic skill. |
| `product_model.py` | Generated Pydantic model output. |
| `bad_schema.sql` | Deliberately invalid schema used to test the database validator. |
| `git_test/auth.py` | Reconstructed final `auth.py` evidence from the Git commit formatter test. |

The original `.git/` folder from `git_test` is not included. The codelab README and screenshots preserve the commit-log evidence.
