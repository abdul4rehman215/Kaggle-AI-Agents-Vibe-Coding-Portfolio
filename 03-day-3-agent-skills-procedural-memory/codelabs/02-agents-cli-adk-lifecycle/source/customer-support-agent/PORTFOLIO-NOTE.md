# Portfolio Source Note

This is the curated source snapshot for the Day 3 Agents CLI + ADK lifecycle codelab.

Kept: application code, tests, manifest, project metadata, and lockfile.

Removed: virtual environments, local run logs, ADK session databases, caches, compiled Python files, and any secret-bearing files.

The project was tested in local Gemini API-key mode. The API key itself is not stored in this repository.
